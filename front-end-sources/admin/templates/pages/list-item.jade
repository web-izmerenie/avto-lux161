th(scope="row" class="js-model-id" data-model-id=id)= id
if is_active
	td= local.get('sections').pages.is_active_yes
else
	td= local.get('sections').pages.is_active_no
td= sort
td
	a(href=ref)= name
td= url
if is_main_menu_item
	td= local.get('sections').pages.is_main_menu_item_yes
else
	td= local.get('sections').pages.is_main_menu_item_no
