.panel-heading
	.row
		.col.col-md-6
			h1.h5= local.get('sections').accounts.accounts_list
		.col.col-md-6.align-right
			.btn-group
				button.refresh.btn.btn-default
					span.glyphicon.glyphicon-transfer
					= ' ' + local.get('sections').accounts.refresh
				a.add.btn.btn-default.btn-success(href="#panel/accounts/add.html")
					span.glyphicon.glyphicon-plus
					= ' ' + local.get('sections').accounts.add
table.table
	thead
		tr
			th= local.get('sections').accounts.id
			th= local.get('sections').accounts.is_active
			th= local.get('sections').accounts.login
	tbody
